
spring:
  application:
    name: item-service
  profiles:
    active: prod
  cloud:
    config:
      uri: http://config-server:8888

management:
  endpoints:
    web:
      exposure:
        include: '*'
